#include <stdio.h>

#define COL 2

int main(void)
{
	int matrix[n][2], n, i, j;
	char query;
	do
	{
		printf("Do you want to write your student code and result?(y/n)\n");
		scanf("%c", &query);
		while(query != 'y' && query != 'n')
		{
			printf("Please write y or n!\n");
			scanf("%c", &query);
		}
		for(i = 0; i < n; i++)
		{
			for(j = 0; j < 2; j++)
			{
				if(j == 0)
				{
					if(
	}
	while(query == 'y');